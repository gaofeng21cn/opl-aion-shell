{
  "name": "opl-acp-extension",
  "displayName": "OPL",
  "version": "0.1.0",
  "description": "Fixture extension that contributes the Codex-default OPL GUI shell.",
  "author": "AionUi Test",
  "contributes": {
    "acpAdapters": [
      {
        "id": "opl-acp",
        "name": "OPL",
        "description": "Launch the local OPL GUI shell on the Codex-default session path.",
        "connectionType": "cli",
        "cliCommand": "${env:OPL_ACP_BRIDGE_CMD}",
        "defaultCliPath": "${env:OPL_ACP_BRIDGE_CMD}",
        "acpArgs": ["--experimental-strip-types", "${env:OPL_ACP_BRIDGE_ENTRY}", "session", "runtime", "--acp"],
        "authRequired": false,
        "supportsStreaming": true
      }
    ]
  }
}
