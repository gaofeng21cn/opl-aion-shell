{
  "name": "opl-acp-extension",
  "displayName": "OPL",
  "version": "0.1.0",
  "description": "Fixture extension that contributes the Codex-default OPL adapter.",
  "author": "AionUi Test",
  "contributes": {
    "acpAdapters": [
      {
        "id": "opl-acp",
        "name": "OPL",
        "description": "Fixture adapter for validating the OPL session runtime wiring.",
        "connectionType": "cli",
        "cliCommand": "${env:OPL_ACP_BRIDGE_CMD}",
        "defaultCliPath": "${env:OPL_ACP_BRIDGE_CMD}",
        "acpArgs": [
          "--experimental-strip-types",
          "${env:OPL_ACP_BRIDGE_ENTRY}",
          "session",
          "runtime",
          "--acp"
        ],
        "authRequired": false,
        "supportsStreaming": false
      }
    ]
  }
}
